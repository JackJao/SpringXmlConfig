<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       " profile="dev">

    <!--使用spring的prifile创建不同环境的数据源信息或其它环境信息，与maven中的profile不同，maven是在打包的时候确定的，
        不同环境需要分别打包（产出多个war包），而spring的profile是在运行时确定使用的环境信息，只需要一个war包-->
    <!--xml配置创建方式有2种：例如：第一种 ，dev环境一个spring的xml文件，prod环境一个spring的xml配置文件，
        分别在跟元素beans中配置属性profile="dev"或profile="prod"-->
    <!--第二种，在一个spring的xml配置文件中，beans根元素下，定义多个beans元素，分别指明使用的环境-->
</beans>